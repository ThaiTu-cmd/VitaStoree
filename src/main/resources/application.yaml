server:
  port: 8080
  servlet:
    context-path: /VitaStore

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/EcommerceDB"
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
